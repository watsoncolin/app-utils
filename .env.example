# Copy to .env and fill in. .env is gitignored — never commit real keys.

# Runware (image generation: app icons + optional screenshot backgrounds)
RUNWARE_API_KEY=rw-xxxxxxxxxxxxxxxxxxxxxxxx
DEFAULT_IMAGE_MODEL=runware:400@2
